name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  call_bao-foundry-project:
    uses: baofinance/bao-base/.github/workflows/CI-test-foundry.yml@main
    secrets:
      ALCHEMY_KEY: ${{ secrets.ALCHEMY_KEY }}
